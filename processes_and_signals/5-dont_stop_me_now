#!/usr/bin/env bash
# Stop the 4-to_infinity_and_beyond process

# Get the PID(s) of the running script
pid=$(pgrep -f 4-to_infinity_and_beyond)

# Kill the process if found
if [ -n "$pid" ]; then
    kill "$pid"
fi
